// Страница "Товары" — управление каталогом. Вся логика и разметка —
// в components/ProductsScreen.tsx, здесь только подключение экрана
import ProductsScreen from '@/components/ProductsScreen';

export default function ProductsPage() {
  return <ProductsScreen />;
}
