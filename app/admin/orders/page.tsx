// Страница "Заказы" — управление заказами клиентов. Вся логика и
// разметка — в components/OrdersScreen.tsx, здесь только подключение
import OrdersScreen from '@/components/OrdersScreen';

export default function OrdersPage() {
  return <OrdersScreen />;
}
