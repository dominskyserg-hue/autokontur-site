// ============================================================
// Проверка "это запрос от залогиненного администратора?" для публичных
// роутов, которые админу отдают дополнительные поля (закупочная цена,
// поставщик): /api/products, /api/products/cross-lookup и т.п.
//
// Раньше здесь сравнивалась cookie с sha256(ADMIN_PASSWORD). Теперь
// сессии хранятся в базе (admin_sessions) — вся логика в lib/adminAuth.ts,
// этот файл оставлен, чтобы не менять импорты в существующих роутах
// ============================================================

export { isAdminRequest } from '@/lib/adminAuth';
